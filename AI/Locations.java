    public class Location{   //, could be a subclass of EnvironmentObject

          private String State;
          private char Location ;  // coordinates
            // could be String or enum, in this case the values are Clean, Dirty
         
          private String getState(){
        	  currentState = State;
        	  return currentState;
          }

          private String setState(){
        	  State
        	  return;
          }
        
}